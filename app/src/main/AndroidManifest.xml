<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ob.Obrowser"
    android:installLocation="auto"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="10" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    This must be a child of the element.

    <application android:icon="@drawable/ic_launcher">
        <activity
            android:name=".ObrowserActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddLink"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="com.ob.Obrowser.ADDLINK" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddLinkActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="com.ob.Obrowser.ADDLINKACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".insertdata"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="com.ob.Obrowser.INSERTDATA" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".datab"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="com.ob.Obrowser.DATAB" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".webdisp"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="com.ob.Obrowser.WEBDISP" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>